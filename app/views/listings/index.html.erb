<div class="map col-md-6">
  <%= gmaps("map_options" => { 
      "detect_location" => true, 
      "center_on_user" => true,
      container_class: "",
      class: "map",
      "zoom" => 6},
    "markers" => { "data" => @json },
    ) %>
</div>
<div class="col-md-6">
  <h3>Your listings:</h3>
  <ul class="list-unstyled">
    <% @listings.each do |listing| %>
      <li><%= render "small_listing", listing: listing %></li>
    <% end %>
  </ul>
  <%= paginate @listings %>
</div>